---
import { buttonVariants } from "@/components/ui/button";
import MobileMenu from "@/components/MobileMenu.astro";
---

<script>
  // Add event listeners for audit buttons
  document.addEventListener('DOMContentLoaded', () => {
    // Function to scroll to the site audit section
    const scrollToAuditSection = (e) => {
      if (e) e.preventDefault();
      
      // Get the header height to use as offset
      const headerHeight = document.querySelector('header')?.offsetHeight || 64;
      const targetElement = document.getElementById('site-audit-section');
      
      if (targetElement) {
        const elementPosition = targetElement.getBoundingClientRect().top + window.pageYOffset;
        
        // Scroll to element with header height offset
        window.scrollTo({
          top: elementPosition - headerHeight - 20, // 20px extra padding
          behavior: 'smooth'
        });
      }
    };

    // Add event listeners to all audit buttons
    document.getElementById('nav-audit-btn')?.addEventListener('click', scrollToAuditSection);
    
    // Expose scrollToAuditSection function to window for React components
    window.scrollToAuditSection = scrollToAuditSection;
  });
</script>

<header
  class="bg-white sticky top-0 z-50 w-full border-b shadow-sm"
  style="visibility: visible; opacity: 1;"
>
  <div
    class="container flex h-16 items-center justify-between"
    style="visibility: visible; opacity: 1;"
  >
    <!-- Logo Section with explicit styles -->
    <div class="flex gap-6 md:gap-10" style="visibility: visible; opacity: 1;">
      <a href="/" class="flex items-center space-x-2" style="visibility: visible; opacity: 1;">
        <span class="inline-block font-bold text-xl text-black" style="visibility: visible; opacity: 1;">
          Search<span class="text-orange-500" style="visibility: visible; opacity: 1;">Serpa</span>
        </span>
      </a>
    </div>

    <!-- Navigation Section with explicit styles -->
    <div class="flex items-center space-x-4" style="visibility: visible; opacity: 1;">
      <!-- Desktop Navigation -->
      <nav class="hidden md:flex items-center space-x-1" style="visibility: visible; opacity: 1;">
        <a
          href="#services"
          class="text-sm font-medium text-gray-600 px-3 py-2 hover:text-black transition-colors"
        >
          Services
        </a>
        <a
          href="#why-different"
          class="text-sm font-medium text-gray-600 px-3 py-2 hover:text-black transition-colors"
        >
          Why We're Different
        </a>
        <a
          href="#pricing"
          class="text-sm font-medium text-gray-600 px-3 py-2 hover:text-black transition-colors"
        >
          Pricing
        </a>
        <button
          id="nav-audit-btn"
          class={buttonVariants({ variant: 'default' })}
        >
          Get Free Audit
        </button>
        <a
          href="https://nxtmt.bloom.io/login"
          target="_blank"
          rel="noopener noreferrer"
          class={buttonVariants({ variant: 'outline' })}
        >
          Login
        </a>
      </nav>

      <!-- Mobile Menu Toggle Button with explicit styles -->
      <div style="visibility: visible; opacity: 1;">
        <MobileMenu />
      </div>
    </div>
  </div>
</header>
